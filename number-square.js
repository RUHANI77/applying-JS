/*
Write a function which will take an integer and will return the square of a 
number
*/
function square(a) {
    let result = a * a;
    return `Square of ${a} is ${result}`;
}
let n = 6;
console.log(square(n));